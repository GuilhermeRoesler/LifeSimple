/** Reexportações para compatibilidade — prefira `@/data/products` e `@/constants/contact`. */
export { initialProducts, productNames } from '@/data/products';
export {
  WHATSAPP_NUMBER,
  INSTAGRAM_URL,
  EMAIL,
  PHONE_DISPLAY,
  ADDRESS,
  ADDRESS_LINES,
  BUSINESS_HOURS,
} from '@/constants/contact';
